{
  "name": "design-to-code-json",
  "version": "0.9.0",
  "description": "Figma plugin: export the selected frame as compact, codegen-ready JSON — node tree, colour/text-style catalogs, and reusable components.",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node tool/build.mjs",
    "test": "node tool/test_transform.mjs && node tool/test_components.mjs",
    "assets": "node tool/render_assets.mjs",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.100.0",
    "@resvg/resvg-js": "^2.6.2",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
